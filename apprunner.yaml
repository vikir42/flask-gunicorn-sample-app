version: 1.0
runtime: python311
build:
  commands:
    build:        
      - pip3 install pipenv
      - pipenv install
run:
  runtime-version: 3.11
  command: pipenv run gunicorn run:app --bind 0.0.0.0:8000
  network: 
    port: 8000
