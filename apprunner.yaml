version: 1.0
runtime: python311
run:
  runtime-version: 3.11
  pre-run:  
    - pip3 install pipenv
    - pipenv install
  command: pipenv run gunicorn wsgi:app --bind 0.0.0.0:8000
  network: 
    port: 8000
